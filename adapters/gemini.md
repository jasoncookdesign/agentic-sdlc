# Gemini adapter

Map roles to isolated Gemini sessions or agent contexts. Ensure the reviewer does not inherit the
builder’s reasoning trace. Bind generic capability profiles in environment-local configuration and
use the generic CLI as the portable artifact interface.

