# Local inference adapter

Local agent environments may map capability profiles to different models, quantizations, context
sizes, or human checkpoints. Preserve independent review even when only one model is available by
starting a fresh context with only the specification, artifact, tests, and review rubric.

The CLI requires only local Python and no network access.

