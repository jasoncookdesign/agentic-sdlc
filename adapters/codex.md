# Codex adapter

Use a Codex task for lifecycle coordination and separate agent or task context for independent
review. Keep `project.json` capability profiles generic and map them to locally available models.
Use the generic CLI for artifact initialization, validation, status, module selection, and verdict
recording.

