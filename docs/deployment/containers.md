# Containers

Pin base images and dependencies, run as a non-root user where feasible, keep secrets outside the
image, define health checks, and produce immutable tagged artifacts with provenance. Verify the
running image digest and application behavior after rollout. Document rollback to a known digest
and any migration compatibility limits.

