# Windows service

Use a dedicated service identity with least privilege, an explicit executable path and working
directory, controlled environment or secret injection, recovery settings, and Event Log or
application logging. Install or update through the organization’s service mechanism, verify service
state and behavior, and document restoration of the prior binary and configuration.

